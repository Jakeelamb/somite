# Implementation Plans

Generated by the improve skill on 2026-08-24. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Repo state at planning:** no git repository yet (unborn). No application code. Docs only: `docs/axial-design.md`, `docs/operator-contract.md`, `docs/catalog.md`, `CONTEXT.md`.

**Audit level:** direction (`next`) on a docs-only tree. Code correctness/security/perf were not audited because there is no code. Verification baseline is plan 004.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Lock playable-instrument product thesis in docs | P1 | S | — | TODO |
| 002  | Specify see-data-flow contract | P1 | S | 001 | TODO |
| 003  | Specify share-without-a-platform | P1 | S | 001 | TODO |
| 004  | Cargo workspace + `axial-ir` verification baseline | P1 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- 002 and 003 are doc/IR-contract spikes. They assume the thesis in 001 (playable window, two surfaces, small tested catalog). Do not execute them against the pre-001 "wrap generator is the product" text.
- 004 may start after 001's vocabulary is in `CONTEXT.md` (crate prefix `axial-*`, graph files `*.axial.json`). It does not need 002/003.
- Canvas implementation (wgpu PRs 10–14 in the design) is **not** in these plans. See rejected item below.

## Findings considered and rejected

- **Wrap generator as the product** (`docs/operator-contract.md` "Product: the bridge"): ships a framework. Novices never `ops wrap`. Rejected as the user-facing thesis. Keep wrap as *expert* DX.
- **CLI-first as the user story** (`docs/axial-design.md` v0.1-cli before canvas): correct as an *engine* test harness, wrong as the thing a novice opens. Engine tests stay CLI; the *product* demo is the window.
- **HoX boutique of eight optimized natives:** solo cannot maintain. Rejected.
- **Empty catalog / users wrap everything:** novices bounce. Rejected.
- **Galaxy untyped connect-anything:** rejected in design; still rejected.
- **nf-core as the default canvas:** 75 modules or one `Directory` blob. Both kill fun. Bricks + optional compound. Rejected as the first demo.
- **Cheap/DOM canvas before wgpu (direction 6):** user did not select. Native wgpu stays. 2k/60 remains a bench report, not a merge gate (`docs/axial-design.md` line 140).
- **Technique marketplace / accounts / sharing servers:** still rejected. Plan 003 is file-based share only.
- **BAM particles / 60 Hz cooks:** still rejected.
- **Implement application code in 001–003:** these are doc/contract spikes. Code starts at 004.
